import {
  mdiLinkedin,
  mdiGithub,
  mdiWeb,
  mdiMenu
} from '@mdi/js'

export default {
  data () {
    return {
      icons: {
        mdiLinkedin,
        mdiGithub,
        mdiWeb,
        mdiMenu
      }
    }
  }
}
